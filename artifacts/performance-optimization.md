# 静态绘制优化与静默浏览器检查

## 当前进度
- 静态方块按材质/阴影属性与8单位空间区域合批，1324个方块变为32组；保留原位置、颜色、阴影、镜头和画质。
- 角色、NPC、门、流水线、流星、灯塔光束与渐变地面没有被合并进静态批次。边界/色彩/共享资源释放测试通过。
- 同GPU小镇直接绘制计数约2095→591；CPU绘制阶段均值约4.64→2.93ms。优化后GPU计时均值约1.81ms（无disjoint），这些是局部观察值，不是完整旅程的帧率保证。
- 三场景三视口像素比较通过：证据见scenery-batch-comparison.json。比较只切换合批，摄像机/颜色/光照不变，没有缩小画面或降低阴影质量。

## 性能测量不能混同
- ego代理窗口出现失焦，按网站规则清空移动输入；若没有走完三景，则该记录不能作为旅程性能通过证据。
- 另用独立有窗口Chrome和模拟测试焦点做同条件对照：未合批P95约10.9ms、合批约10.8ms，最终一次约10.4ms，包含完整30秒与三景。不能把早期ego的40–50ms到Chrome的10ms差异全部算作代码优化收益。
- 这些有窗口记录与失败尝试均保留于release-performance.json，按source/runLabel区分，没有删除失败帧。
- 初轮ego全量回归198通过、6失败，主要是自动行走中止；改用无窗口原生Chrome后，修正测试驱动迁移问题，最终完整206项全部通过。历史失败不删除，但不继续作为当前版本的验收状态。

## 用户最终要求：默认静默后台
- 不再主动激活浏览器、不抢用户焦点，不为了测试时序执行open -a或Page.bringToFront。
- tests/headless-browser.ts供全部浏览器测试共用，仅使用--headless=new和临时独立资料目录。最终30秒无窗口三景行走1802帧、P95 16.7ms、最大16.8ms，通过33.4ms门槛；记录标为headless-final-all。**此前有窗口性能数据不作为无窗口测量数据**。
- tests/browser.ts通过无窗口Chrome发送真实输入事件；独立实例使用临时资料目录，结束后关闭并清理。
- 全局AGENTS.md记录静默要求，~/.pi/agent/mcp.json已为chrome-devtools加入--headless/--isolated。用缓存的官方1.8.0服务及这些参数完成真实MCP握手、list_pages、evaluate_script，确认HeadlessChrome后关闭实例；用户重载Pi后，当前工具连接也已验证HeadlessChrome，后台创建的复核页已关闭。
- 无窗口通道完整验收：206项通过、0失败、9017断言（41文件），构建/类型检查通过；包括三景往返、门、双语阅读、换装、彩蛋、故障恢复、真实可信输入和失活清理。未再次提交、推送或上线。

## 清理
原始性能JSON与像素比较JSON为保留证据。本次临时截图、CPU采样、日志和dist清理，自启4173预览停止，原3000服务不动；用户浏览器窗口/资料目录不操作。
