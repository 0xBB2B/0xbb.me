# 旧代码与临时材料复查

## 提交与范围
先按用户要求将此前已确认的跳跃、图标和设计目录清理成果提交为`59929d2`。本报告记录该提交之后的残留复查；这些新清理改动尚未再次提交，未推送或上线。

## 已清理

### 确认无用途的临时文件
- `.pi-spec/.cache/t33-*`：42个旧对比截图、修改前源码备份及差异文件，约6.4MB。
- `/tmp/0xbb-home.html`：旧CyberDeck首页的临时抓取结果，包含过期标题和分享图引用。
- 仓库根`.DS_Store`。
- `design-reference/`、旧游戏源码、旧导出模型在前序提交中已经移除，本次再次确认不存在。

### 临时文案和重复定义
- 去掉首页“人物造型预览”提示及对应UI节点/CSS/中英字段，正式移动说明仍在页脚。
- 删除UI_COPY中无人引用的town/chapter/title/welcome字段；实际三章文案继续由SCENE_COPY提供。
- 删除无引用的greeterPages导出别名。
- 包名称改为`mc2d-portfolio`；仅修改package与锁文件的项目名称，依赖解析内容不变。
- CodeGraph本地生成目录加入.gitignore，索引本身继续使用，不删除。

### 模型检查
- 黑装模型入口为`portfolio/models/player-voxel-black.ts`，使用几何与顶点颜色。
- 清理前后对每块网格的世界坐标顶点、法线、颜色、索引及阴影/材质属性进行指纹比较，数值按1e-6精度量化：
  `d33563e29c04ed6338dee1ef202df5847efd0f8bb81643d43c4e086550262793`
- 前后指纹一致，尺寸、五官、服装细节、走跑/跳跃接地检查通过，不改变用户确认的造型和动作。

### 验收记录
- `.pi-spec/.../acceptance.md`汇总实际本地检查，并明确列出未覆盖的手机真机、人工审核与发布事项。

## 当前资源用途
- `public/profile-full.png`：正式资料速览使用的人设图。
- `.pi-spec/.cache/dev-server.log`和`dev-server.pid`：正在运行的原3000开发服务实际使用，PID对应40722/40723，不删除、不停止服务。
- `.codegraph/`数据库与守护日志：当前代码索引所需，不是临时对比材料；忽略Git跟踪。
- `artifacts/release-performance.json`、像素对比JSON和正式文字报告：已执行验收的证据，不能为了工作区整洁抹掉历史失败记录。
- `.pi-spec`的需求、决策和历史任务：审计记录，不作为旧运行代码清除。
- 首页与交互测试用于检查入口、语言、移动、阅读和状态连续性。

## 公开图片
`public/profile.png`（约2.4MB）和`public/profile.jpg`（约569KB）已获用户确认作为公开静态图片，与profile-full.png一起进入构建结果。文件名和原始字节不变，分别通过同名URL访问。

## 验证
- 82项相关测试通过、0失败、6012断言（14文件），覆盖模型一致性、走跑跳跃、阅读、换装、图形失败和资源边界。
- 收尾17项模型/清理/文案/发布复验通过；构建、冻结安装和`tsc --noEmit --noUnusedLocals --noUnusedParameters`通过。
- 依赖锁定解析前后完全相同，没有新增依赖；本次不是重新执行全部历史测试。
- 测试始终使用无窗口Chrome，不激活ego或前台窗口。
- 本次生成的模型草稿、清理清单、测试日志和dist已清理；正式证据与原开发服务保留。
