---
name: hd2d-portfolio
title: MC-2D 横版探索个人主页
status: executing
created: 2026-09-05
---

# MC-2D 横版探索个人主页

## 1. 背景

访客需要通过操控主人公、沿道路探索并与 NPC 交谈，认识 FUBUKI_BB 的背景、技能和作品。当前没有现行规范覆盖这一体验，需要先建立明确、可验收的行为基线，避免将普通网页装饰成游戏或在方向未验证时铺满场景。

本次按需求变更建立新需求包，不是既有游戏故障修复。需求按里程碑实施，当前进入 M1 规划与实现；文档确认不代表产品已经实现或完成黑盒验收。

## 2. 目标与非目标

### 非目标

- USER-081已授权先提交当前成果（fc824b2），再执行M3/T-31、T-32发布前收尾；USER-082/083追加T-46服装彩蛋；USER-084至087要求性能优化并全程静默无窗口，T-47已完成静态合批与无窗口测试驱动，完整206项验证通过。只做本地修改和验证，不推送或发布；不新增未经批准的依赖。允许相关代码、配置、测试和文档修改，以及通过验证后的本地提交。
- 不加入战斗、死亡、必须跳跃的障碍、背包、任务树、音频、后台服务、鼠标点击地面寻路或跨刷新存档。
- 不升级现有依赖，不改造部署平台，不制作原画级动画，不因没有测量依据的猜测进行性能重构。

### 目标

- 交付 3 个连续场景：黄昏城镇、地下数据工厂、蓝色荧光海；迎宾NPC介绍背景，五块无等级技能终端与三颗空中项目星星介绍技能和作品，在海边灯塔旁收尾。交谈与查看按钮显示在对象旁边或头顶，随场景移动。
- 主人公与全部NPC统一采用Minecraft方块三维像素风格；默认黑装主角自然闭嘴无舌、整体为原80%；灯塔旁可带特效切换黑装与A黑裙，B为NPC，不提供C模型。人物移动预览与完整待机/行走动画分别验收。
- 三维人物仍用无图片纹理的方块几何，背景/道具/图标用SVG或几何；用户确认的profile-full.png只作为资料速览HTML人设图按需加载，不用作角色或场景纹理。
- 提供中文与英文 2 种界面及介绍，以及覆盖相同资料的速览入口；图形故障不阻断获取资料与外链。
- 在 1440×900、390×844、844×390 三种视口验证布局和操作；桌面记录设备与浏览器，连续测量 30 秒，目标 60 FPS，帧间隔 P95 不超过 33.4 ms。

| 里程碑 | 交付与验证 | 停止点 |
|---|---|---|
| M0 文档基线 | 8 条独立规则、逐项验收、决策台账、里程碑；文档 lint 通过 | 用户确认并提交规范后进入规划；文档通过不代表产品验收通过 |
| M1 可玩样板 | 先确认静态人物稿，再接入全身像素角色动画；人物统一Minecraft三维几何，黄昏城镇与UI使用SVG或几何，支持1位NPC、键盘与触屏、双语和速览，跑通行为测试并录制效果 | 用户确认角色辨识度、步态、立体场景与手感后才扩建；M1 不代表整站验收完成 |
| M2 完整旅程 | 3 场景、1 位 NPC、5块无等级技能终端与3颗项目星星、海边灯塔、4 个社交入口、双语全量资料速览，两个交界处可往返 | 验证全程、对话、语言与外链，不在内容未齐时追加特效 |
| M3 发布验收 | 图形故障阅读、三种视口、静态构建、旧游戏移除、世界矢量/几何产物及单张人设图例外检查、第三方授权核验及桌面性能测量 | 完整黑盒验收通过后才申请发布；手机真机性能无证据时标为未验证 |

## 3. 规范变更

| 规范 | 变更 | 说明 |
|---|---|---|
| portfolio/bilingual | 新增 | 默认英文，中英切换覆盖界面与内容且不重置探索或对话位置。 |
| portfolio/graphics-runtime | 新增 | 图形加载、故障阅读及可复测的桌面帧间隔门槛。 |
| portfolio/npc-dialogue | 新增 | 迎宾交谈、看板摘要与主动详情、阅读暂停、关闭和互斥。 |
| portfolio/player | 新增 | 默认黑装与灯塔旁A黑裙切换、Shift快跑摆幅、输入释放与阅读暂停；默认尺寸和造型不变。 |
| portfolio/profile-overview | 新增 | 完整资料、五项无等级技能、作品与社交外链的直接阅读入口。 |
| portfolio/responsive-layout | 新增 | 三种视口下的可见操作、正文阅读与尺寸变化。 |
| portfolio/site-entry | 新增 | 探索图形纯矢量/几何，资料速览允许用户确认的人设图例外，不使用模型图片纹理。 |
| portfolio/world | 新增 | 暖光城镇、地下数据工厂、无街区建筑的蓝色荧光海连续衔接，技能说明嵌入背景终端屏幕，三颗项目星星、灯塔和木板路明确尽头。 |

## 4. 边界与已知坑

### 授权与阶段门禁

- USER-013 已确认规范并授权规划、M1 工作流和对应本地提交；USER-027/028确定只保留黑装、去掉舌头和切换按钮，人物等比缩到原80%；非发布参考材料边界不变。不得因规划包含后续里程碑就执行 M2/M3。产品验收状态由全部任务及黑盒验收门禁决定。
- 规则文件是产品行为事实源；此处只记改动目的、工程边界和交付门禁，不替代逐条验收。当前任务集按 USER-016/AI-009 由主 agent 完成已定切片的文档整理，明确 M1、M2、M3 分组与 M1 停止点；所有任务均需通过Red、Green、自验收与结构化门禁，完整分支须经过独立审查。
- 按任务由pi-spec.worker严格执行Red、Green与自验收，主agent通过结构化门禁后复跑verify才允许提交；完整分支白盒审查在所有任务完成后执行。M1 的用户视觉确认是额外门槛，不能用自动测试通过替代。
- 性能改动须附测量环境、未达标指标、定位证据和改动后的同条件结果；缺少任一项不开展性能专项重构。M1 美术不通过时先修样板，不扩建后再重做。

### 已确定的规划边界

- USER-049从独立三维对比页选择B地下数据工厂与2蓝色荧光海，要求木板路在灯塔旁像起点一样明确结束，右侧不再铺装。USER-050要求技能描述做在背景模型的数据终端屏幕内，而非悬浮网页卡片；文字和简介与中英资料一致。
- USER-051至056要求中间房间用垂直于屏幕的侧门、道路贯通且进出前不能提前看到另一侧；修正首次停顿、门宽/穿模，末段减半，并加入流水线/流星/旋转柔光、超宽屏和外墙细节。用户明确仅满意第一章小镇，要求使用已安装Three.js skills打磨后两章的光影、星体与海面。
- T-40保留小镇/主角和B＋2的内容组织，不把旧后两章外观当已定稿；采用侧墙上卷门、严格跨门显示、预热、近岸程序化水面、柔光星核、砖厂外观和批量绘制，终点70.5。
- USER-057至060通过T-41明确EN/中只在主页无弹窗时显示；修正星海/浪向和墙角，增加灯塔题记与多段文案。关闭后在主页换语言再阅读，故障资料也可关闭/重开，不恢复弹窗语言按钮。
- USER-061至063通过T-42确认主角称谓AI Agent开发者、原住民NPC世界观与独立造型；用户随后否定A/B候选，沿用原NPC并略放大眼睛。
- USER-064至066授权T-43在人设档案中使用profile-full.png，按用户补充的实际经历重写五项技能，并将第三章改为星辰大海的表达且与01/02标题对齐。主角/NPC模型、地图和控制保持。USER-067确认风格名称为MC-2D，页面标识、画布可访问名称与中英作品介绍统一使用该名称。不进入M3或发布。
- USER-068至077补充T-44：技能最多三列；灯塔处保留左上标题，寄语横屏塔右/竖屏塔左；流星改细长渐隐；Shift快跑提速且加大手脚摆幅。NPC选B金发浅外套，删除原浅蓝NPC及旧对比模型；主角只切换黑装/A，C删除。换装按钮与灯塔寄语同时出现/隐藏，带0.65秒局部光点动画，位置和语言不变。
- USER-080要求快跑身体上下起伏更轻更慢：两款主角均改为一次摆臂周期对应一次平滑起伏、范围0.025单位，腿部承担接地补偿；移动速度、手脚摆幅/节奏及普通步行不变。
- USER-082/083：按钮改为切换服装；穿A首次主动找迎宾者交谈，显示1秒感叹号、打字赞美和短暂文字停留，结束再恢复交互。每次刷新只触发一次，普通黑装交谈不消耗彩蛋，图形故障仍能读取资料。
- 五项无等级技能、三个作品和外链、双语资料/故障阅读不变；只迁移被本轮视觉要求替代的测试预期，保留有效交互/故障/接地回归。T-30已完成记录作为历史，不改写为新方案验收。

- USER-039批准T-29单城镇故障阅读与静态交付；AI-033隔离图形加载和资料UI、刷新恢复，不自动重试；加入无人物纯SVG站点图替换旧PNG分享引用，保留原图字节迁移。完整三视口/静态服务/图形失败注入与现有回归，冻结主角NPC步态房屋镜头，不加依赖、不删game源码、不提交music既有删除；完成后停在M1确认，不推进M2/M3。

- USER-032/033/034批准T-28：棕绿Minecraft店主，横向≤1.8提示；三页身份地点/工程/AI背景，重读第一页；面板互斥，Esc关闭，语言保持段落，阅读暂停且新输入恢复。AI-029选择x=2道路后侧站位，单一会话源通知UI。完整双语资料仅现有事实，更新已过时动画提示；角色/GLB/步态/房屋/镜头/速度冻结，不扩景或新增依赖。

- USER-030确认当前造型并批准续做T-27真实步态：双腿交替、手臂配合与左右朝向，松手/失焦/阅读停止；保留黑装闭嘴无挂件、80%尺寸、房屋/镜头/速度，不做NPC或扩景。AI-027限定修改portfolio/character.ts与新增character.test.ts，复用已有状态和输入，静态模型/GLB与现有测试冻结。仅本轮动作测试与实际三视口步态通过后才标记该任务，不宣布M1全部完成。

- USER-029确认仅删除双靴蓝色挂件、挂链和挂件高光，保留鞋带、鞋扣、厚底、蓝色耳饰、闭嘴表情与80%尺寸。源模型和真实GLB同步，预览说明不得继续宣称带蓝饰；不改场景、镜头、输入或其他衣装。按AI-025增量规划T-36，T-35已完成记录不回写为新要求通过。挂件删除已由T-36完成，后续范围以USER-030的T-27步态授权为准。
- AI-026核实预览HTML/TS无蓝挂件文案后，T-36写入范围为design-reference/player-voxel-black.ts、player-voxel-black.glb和character-design.test.ts，预览HTML/TS只读核验。必须先用已有模型和真实GLB验证挂件仍存在而得到行为Red，再删除，重新导出并实际回读GLB。保护其余几何、2.4高度、脚底原点及三视口/移动回归；不改基础player-voxel.ts、浏览器helper或现有输入测试。沿用现有Bun/ego，不加依赖。
- planner因旧硬限制未产出任务，AI-026沿用既有主agent切片整理授权新增T-36并更新INDEX；进行中T-27的C-1/AC-1原文同步现行规范，已done的T-35按其提交保留验收历史，不修改其内容/状态/commit。新任务完整文件仍≤200行；允许覆盖本条明确列出的生产文件和测试范围，不受默认2文件限制。

- USER-027/028授权T-35仅显示黑装Minecraft、自然闭嘴无舌、取消所有人物/白装切换入口，模型整体等比0.8（高3变2.4），脚底原点/头身比例保持。不改房屋、镜头或移动速度，不扩建场景或动画；T-34旧双模型结果不作为新要求通过。
- 单模型页只查看/导出黑装；基础player-voxel.ts仍被黑装构造实际复用，不作为用户可选白装模型，不复制骨架或新增兼容选择。黑装源与GLB同步2.4单位，当前任务需真实Red→Green→自验收及主agent复验。

- USER-025恢复ego-browser；原Bun测试helper和26项回归已实际通过，AI-019取消尚未落盘的Chrome测试迁移。继续使用真实Bun/ego测试，不新增驱动或依赖；最初的工具缺失、探测脚本错误都不计Red。
- 按当前安装的spec-flow由pi-spec.worker在一个任务内完成Red→Green→自验收，主agent核对structuredOutput并复跑verify。所有任务完成后才做整个分支的独立白盒审查和黑盒验收；旧wf记录只供追溯，不代替新任务门禁。



- 当前黑装参考为 `design-reference/player-black-reference.png`，SHA-256为00684f82856442c65e9838a4623ea88273ec0c96bb055c107539aa01f2d705d5。主角采用银白高马尾、蓝眼、自然闭嘴表情、黑短外套/黑短上衣、腰部肤色、黑短裤、单侧袜及无挂件/挂链的厚底系带靴。
- 独立预览入口 `/design-reference/character-comparison.html`；设计文件结构校验命令 `bun test ./design-reference/character-design.test.ts`，不能将其视为正式游戏TDD或完整视觉验收。T-35按USER-027/028将单黑装人物用于主页预览，用户选择方向后再推进正式动画；本次不把静态位移宣称为行走动画。

- USER-016 授权按完整用户行为重新规划，AI-008 明确六个串行切片和浏览器 Red 入口；以下代码分层是实施组织，不再按每个内部模块拆独立任务。不为单测可导入而创建生产空实现。
- 资料层：`data.ts` 管理中英个人事实，`portfolio/copy.ts` 管理界面词条，不依赖图形运行时。
- 交互层：`portfolio/state.ts` 与 `portfolio/input.ts` 管理移动、阅读、语言、输入释放和焦点中断，公开接口可由 Bun 行为测试调用，不嵌入渲染或静态页面内容。
- 图形层：`portfolio/geometry.ts` 提供场景几何，`portfolio/character.ts` 负责黑装/A主角的姿态与接地，`portfolio/avatar-models.ts`提供A/B几何，`portfolio/appearance-effect.ts`提供换装特效，不加载SVG或位图人物；`portfolio/world.ts` 与 `portfolio/runtime.ts` 负责场景组合、相机、光照及图形生命周期。
- 界面层：`components/portfolio/` 下按 WorldViewport、Hud、Dialogue、Overview 分组件，组件样式由同一任务负责；`App.tsx` 只组合主页，`index.css` 负责基础样式。无须新状态库、游戏引擎或 UI 库。
- 场景层：`portfolio/scenes/` 的场景模块分别拥有布景、NPC 和中英台词；M1 只有 town，M2 才添加 workshop/gallery，不预制未来场景或空实现。独立场景组合沿用现有 Vite 能力；后续串行任务可以调整已声明的组合入口，不为避免文件复用而增加抽象。
- 发布边界：素材发布配置、页面模板、元数据与参考图迁移分别明确列入文件归属；M1 只做样板可运行及纯矢量/几何图形边界所必需的变更。M3 的整体清理须列出确切文件，不能用脚本掩盖大批未声明的源码改动。
- 验证组织：首个可信 Red 从现有网页 `http://127.0.0.1:3000/` 建立。Bun测试通过已有ego-browser实际操作页面并执行断言，同时运行本地模型/数据检查，先验证服务器与浏览器可用，再对用户可见的画面和交互断言；不能导入尚不存在的模块使测试提前退出。后续可在已有公开入口上补单测；截图/录制与视觉核验不以测试钩子或源码关键词替代，不新增测试依赖。
- 文件归属：USER-016 允许一个任务覆盖完整行为所需的文件及验收，不设生产文件或 refs 的数量上限。有明确依赖的串行任务允许修改同一文件；并行任务文件必须不相交。每个任务仍列全生产和测试路径以及删除目标，禁止越界改动。
- 任务文档长度：按 USER-015，每个 `tasks/NN-*.md` 文件的完整内容 ≤200 行，包括 frontmatter、标题、正文、空行和代码块；该上限只针对任务文档，不限制任务修改的代码量。职责、明确文件范围、验收覆盖和 TDD 门禁控制任务规模；共用 skill 和外部仓库不修改。
- 测试角色只读业务规则与验证方式；这两个区块必须内联可写测试路径、独立命令、真实网页入口、具体输入、预期及错误场景。不得再次把这些必要信息仅放在 frontmatter、涉及文件或函数清单内。
- 重规划编号：T-27 M1 可操控黄昏城镇（人物、道路、镜头、键盘与触屏）；T-28 M1 主动 NPC、中英切换与资料速览；T-29 M1 静态样板交付、人物素材边界及故障/三视口集成；T-30 M2 三场景完整旅程；T-31 M3 旧功能清理；T-32 M3 整站验收与说明。六任务串行，全部44条验收映射到相应切片及T-35，M1部分覆盖必须明确标注。
- 旧T-1至T-26不再执行，旧任务文件已由T-27至T-32替换；旧台账与工作流运行证据保留供追溯，延迟回执不得推进新任务。现有 `plugins/htmlPlugin.test.ts`、`public-assets.test.ts` 归入T-29，其他已写测试也必须显式归属或报告，不能删除有效测试来规避失败。
- 如需增加上述边界之外的新架构、依赖或授权范围，planner 先返回具体的未定案提议，由主 agent 决策并取得回执后再写盘；若硬约束存在冲突，返回确切冲突文件、验收与拆分建议，不伪造可执行计划。

### 数据、素材与部署

- 当前事实来源为 `data.ts`：FUBUKI_BB简介、USER-042/043指定的5项无等级技能、3个ONLINE作品、4 个社交入口。只翻译与调整叙述视角，不增加未经确认的个人经历；站点自身作品介绍随新体验更新。
- `public/profile-full.png` 为1696×2528 RGB PNG，用户确认作为资料速览人设图，原图不改且不作为运行精灵或三维纹理；基础几何参考来自USER-019提供的1694×2528全身图，该原图和保存副本SHA-256均为06dcaa59ba14d4eb918266d9b2bcdf16ef692bd3d60f5afaff753ec27d323cc2。黑装参考及其校验值见上文，最终表情和尺寸以USER-027/028为准。
- 主角与NPC均为Minecraft方块三维几何；普通背景、图标、道具和UI可用SVG、CSS或几何。人物模型禁止SVG或图片纹理；除已确认的HTML人设图外，不加载其它位图，不使用Base64或SVG内嵌位图；静态模型移动不代表完成步态。
- 浏览器或图形引擎正常的屏幕栅格化不视为位图素材。除已确认的资料速览人设图外，其它设计参考和测试截图仅作非发布材料，不进入页面图形引用；无图形网络请求也不能代替对内联内容的检查。
- 采用已选定的像素角色加立体场景路线，计划使用现有 React、Vite、Three.js 与 Bun 工作流；开源库及已有能力优先，不自行搭建通用游戏引擎，不新增未经批准的依赖。
- 后续移除范围包括旧 HUD 首页及其专属样式、光剑 `game/` 与 `components/beat-saber/`、专属音频和失去引用的依赖、游戏构建入口及元数据分支；只清本次替换产生的残留，不波及无关工具。三维人物遵循Minecraft方块风格；资料速览使用用户确认的人设图，普通图标仍用纯SVG/几何，其余原始设定图按非发布参考处理。
- 仍为无账号的单访客浏览器体验，无服务器数据写入或多人同步。部署继续使用现有纯静态站点方式，不调整平台。
- 外链验收核对目标地址及浏览器导航行为，不把第三方网站可用性作为本站能够保证的结果。

### 失败与测量

- 加载中允许进入资料速览；必要图形或角色资源报错、图形不支持时展示故障说明与资料，不使访客只能等待。恢复可由访客刷新页面触发，不引入后台服务或自动重试系统。
- 首次默认英文，语言切换不重置位置或当前对话段落；刷新从起点及默认语言开始，不承诺跨刷新恢复。
- 记录实测设备、操作系统、浏览器版本和视口。桌面性能采样在加载结束、页面可见且连续行走期间持续 30 秒；包含跨场景时的帧间隔，不把加载时间或手机模拟当作真机性能数据。
- 390×844 与 844×390 的视口模拟仅验证布局及触屏交互。手机真机性能尚无设备和实测证据，不作为已完成项，也不得在发布报告中冒充已验证。

### 开源调研与使用边界

| 来源 | 借鉴内容 | 已核实与待核实 |
|---|---|---|
| [Three.js 官方示例](https://github.com/mrdoob/three.js/tree/dev/manual/examples) | 几何布景、光照、阴影与相机；通过代码实现空间表现，不使用示例位图 | 已通过官方文档查询核对示例；现有依赖 LICENSE 已读，为 MIT；M0 未复制示例代码 |
| [Pixel-Quest](https://github.com/StarKnightt/Pixel-Quest) | 仅借鉴横向镜头、立体布景配二维角色、动画脚底锚点和预加载经验 | 搜索发现的经验参考，尚未逐文件审查代码许可证；不采用其位图或精灵表 |

- 开源代码、算法、纯矢量SVG及人物素材采用前须记录来源、版本、许可证及要求的署名；代码许可证不自动覆盖图形和字体。当前Minecraft模型为依据用户参考制作的原创几何，不复制游戏贴图或将参考位图作为三维人物贴图，不引入许可证不明内容。
- 仅参考横向移动、相机跟随、动画锚点等与本需求有关的经验，不因参考项目使用了完整物理系统、状态库或编辑器就引入它们。
