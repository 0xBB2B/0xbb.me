---
id: T-24
title: M3 静态交付、无位图检查与项目说明
depends_on: [T-10, T-11, T-12, T-13, T-14, T-20, T-23]
files: [/Users/bb/Projects/0xbb.me/README.md, /Users/bb/Projects/0xbb.me/release.test.ts]
refs: [portfolio/site-entry/AC-3, portfolio/site-entry/AC-5, portfolio/site-entry/AC-6]
parallel: false
verify: cd /Users/bb/Projects/0xbb.me && bun run build && bun test ./release.test.ts && bunx --no-install tsc --noEmit
status: todo
step: test
agent: ""
commit: ""
note: ""
---
## 1. 目标
在 M3 对完整静态交付、代码化图形及许可证据执行可重复检查，替换旧游戏项目说明，不发布或推送。
## 2. 业务规则
- C-3：系统应通过命令 bun run build 生成可静态托管的页面与资源；访客浏览主页、交谈、切换语言和打开速览不需要账号、服务端存档或独立后端应用。
- C-5：系统应仅用程序化图形或纯矢量 SVG 交付主人公、NPC、背景、道具、特效、头像、图标及交互 UI，动画由代码驱动；成品不得包含 PNG、JPEG/JPG、WebP、GIF、BMP、AVIF、TIFF 等位图素材或位图精灵表。正常屏幕栅格化不视为位图素材，设计参考及测试截图不得进入发布产物。
- C-6：如果本站图形以内联数据、矢量文件或外部链接形式呈现，系统仍应仅使用程序化或纯矢量内容，不得嵌入或引用位图，也不得通过 Base64、修改扩展名或 SVG 容器绕过限制。
### AC-3 纯静态构建 ← C-3
- 触发: 命令 bun run build，并通过静态服务打开构建结果。
- Given: 已安装项目约定依赖，构建环境可用。
- When: 执行构建，再仅用静态服务访问主页、NPC 对话、语言切换和资料速览。
- Then: 构建退出码为 0；上述功能无需独立后端应用、登录或服务端存档即可使用。
### AC-5 无位图的图形成品 ← C-5
- 触发: 命令 bun run build，并检查生成的图形产物和完整页面。
- Given: 构建成功，桌面和触屏均可访问全部场景、NPC 与阅读界面。
- When: 检查构建产物的实际图形格式，操作主人公、NPC、全部场景和交互 UI，查看头像、图标与页面图形引用。
- Then: 所有成品图形为程序化图形或纯矢量 SVG，行走及交互动画正常；位图文件与位图精灵表数量为 0，原始设计参考和测试截图没有进入发布产物。
### AC-6 不以编码或容器嵌入位图 ← C-6
- 触发: 操作 在浏览器中访问所有场景和阅读界面，检查收到的图形响应、SVG 内容、样式图形引用及内联图形数据。
- Given: 构建产物由静态服务提供，浏览器能记录本站资源请求和响应内容。
- When: 核对图形内容而非仅文件后缀，展开矢量或内联图形数据，检查本站图形的外部引用。
- Then: 不存在 Base64 位图、SVG 内嵌位图、被伪装格式的位图或外链位图；即使没有图片网络请求，内联内容也满足无位图要求。
## 3. 涉及文件
- 修改 `/Users/bb/Projects/0xbb.me/README.md`：当前体验、操作、开发/验证命令及依赖授权说明。
- 新建 `/Users/bb/Projects/0xbb.me/release.test.ts`：产物内容与说明契约检查，只读检查，不自动删除任何源码。
## 6. 函数清单
无生产函数；测试检查真实构建结果的文件内容、矢量引用与内联内容，而非生产源码关键词替代行为。
## 7. 协作关系
此任务无源码修复权；发现产物/浏览器 FAIL 由主 agent 按归因与门禁流程处置。使用现有 Bun，无新扫描依赖；删除/替换的所有生产文件归前置任务。手机真机性能没有证据就写未验证。
## 8. 验证方式
- 测试写入授权：M3 获授权后，仅新增或修改 `/Users/bb/Projects/0xbb.me/release.test.ts`；测试阶段不得改 README、源码、配置或任务字段。
- 独立验证命令：`cd /Users/bb/Projects/0xbb.me && bun run build && bun test ./release.test.ts && bunx --no-install tsc --noEmit`。
- 公开验证目标：真实 `dist/` 静态交付、网页 `/` 与 `/game/`、公开 README 内容，以及浏览器收到的资源和内联图形；说明文案的全部必须出现/不得出现内容已经内联在本验证区，不需读取其它任务区块来建立预期。
- build 与类型检查成功；静态服务访问 / 可走完三场景、交谈、双向切语言和打开完整速览，不需独立后端。直接 /game/ 不能启动光剑，无站内游戏引导。
- 对每个真实构建文件识别实际内容格式，检查 PNG/JPEG/WebP/GIF/BMP/AVIF/TIFF 魔数、图形内联数据与嵌套 SVG 外部引用，不仅检查后缀。测试用内存负例覆盖改名位图、Base64 位图、SVG 内嵌位图、外链位图；禁止把夹具写进 public。
- ego-browser 浏览全部场景、NPC、双语面板，核对收到的图形响应及 CSS/SVG/DOM 内联引用；没有图片请求不能直接判定无位图。人物、NPC、道具、头像、图标与动画均须实际观察。
- 检查设计参考与截图不入 dist，原图迁移后摘要一致；保留已安装库许可证，逐项核对实际复用来源/版本/许可/署名；许可不明不得发布。只借鉴、未复制的项目明确不列为引入依赖。
- README 必须出现的句子：
  - “这是 FUBUKI_BB 的 HD-2D 横向探索个人主页。”
  - “按 A/D 或左右方向键行走，按 E 与附近 NPC 交谈；触屏使用左右与交谈按钮。”
  - “资料速览无需行走或完成对话；图形不可用时仍可阅读完整资料。”
  - “使用 bun run dev 启动开发，bun test 运行测试，bun run build 生成静态产物，bun run preview 预览构建。”
  - “所有成品图形由代码或纯矢量 SVG 生成，设计参考不进入发布产物。”
  - “手机真机性能尚未验证；视口模拟只验证布局与触屏交互。”
- README 必须不再出现的句子或片段：
  - “Tactical HUD 风格个人作品集 · 独立 3D 节奏光剑页”
  - “独立游戏页 `/game/`”
  - “bun test game/”
  - “public/music.ogg”
  - “游戏页在移动端自动展示占位卡片”
- 文案测试逐条断言上面清单；更新其余旧玩法、旧目录和多页部署说明，不能添加兼容/弃用过渡说明。浏览器与许可门禁缺证据时不报完整通过。
